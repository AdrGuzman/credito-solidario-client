import { BusquedaUsuarioPipe } from './busqueda-usuario.pipe';

describe('BusquedaUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
