import { InhabilitarSiNoAutorizadoDirective } from './inhabilitar-si-no-autorizado.directive';

describe('InhabilitarSiNoAutorizadoDirective', () => {
  it('should create an instance', () => {
    const directive = new InhabilitarSiNoAutorizadoDirective();
    expect(directive).toBeTruthy();
  });
});
